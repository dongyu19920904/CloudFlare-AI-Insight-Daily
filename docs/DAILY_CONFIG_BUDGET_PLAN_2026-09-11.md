# 日报配置等值精简方案

## 已确认问题

- 安全工作区：`D:/GitHub/_worktrees/daily-config-budget-20260911`，基线 `237c69e4d`。
- 部署运行 `34498176575` 被 Cloudflare 10055 拒绝：85 个变量超过 Free 的 64 个限制。当前 TOML 有 77 项文本配置，另外 8 项由部署端提供。
- 不整体回滚旧配置：早期版本自身也超过限制，而且会丢失账号商机独立发布目标等配置。

## 最小实施

1. 将 32 项固定、非敏感参数的原始字符串值移到 `src/workerConfig.js`。
2. `src/index.js` 的 scheduled、fetch 两个入口在校验或路由之前合并默认值。显式环境值优先，KV/密钥绑定不变，不修改传入对象。
3. 从 TOML 删除对应 32 项；其余配置和 triggers 原样保留。文本变量降至 45，若部署端仍有 8 项，总计 53。
4. 测试默认值、显式空值/false/0、绑定引用、双入口与定时路由。用 TOML 解析器比对旧配置和合并后配置全部键值及类型。
5. 全量测试通过后 SSH 推送，沿原 GitHub Actions 流程部署。注意本次部署也会带上之前已提交、被变量上限阻挡的趣闻/证据修复，不再追加文风修改。
6. 只做一次真实 dry-run，核对生成结果和未发布标志，不覆盖线上文章。记录部署版本与剩余问题。

## 边界和回滚

不改提示词、前端、cron、模型及线路、来源列表、发布目标，不删除密钥，不升级付费套餐。只改变配置存放位置，不增加网络调用。

异常时停止正式生成；可用 Cloudflare 上一部署版本回退运行。不能直接恢复 85 变量的 TOML 后重新部署，那会再次超过限制。若仅回退上一阶段内容修复，需保留本次等值配置精简。
