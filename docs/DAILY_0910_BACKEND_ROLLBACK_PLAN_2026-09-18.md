# 2026-09-18 后端回退与当天日报验收

## 核实

- 9 月 9 日与 10 日日报使用同一旧版 Worker 基线 `ea78b401f63ad00807f79ead6bfd181f6ed987e5`；当前远端 `main` 为 `6cd9b783f4ab53d573fede6182d9e2f2225e095b`。
- 旧版直接上传会超过 Cloudflare 免费版 Worker 变量上限；隔离试点提交 `2b650d1` 只加入等价的配置收拢，配置等价检查已通过。该试点用 9 月 18 日缓存生成时曾因非当日 Trending GitHub 项目进入 TOP 而未通过发布校验。
- 9 月 18 日中文文章目前已有人工校订版，用户确认质量满意。前端文章与历史内容保留；任何新生成稿须先验收，不能直接覆盖它。

## 实施

1. 从最新远端建立独立 `codex/` 工作区，将 Worker `src/`、测试与 `wrangler.toml` 恢复到 `2b650d1` 的旧版代码和等价配置。保留现有部署工作流与历史文档，避免再次执行 9 月 15 日回退提交或改动前端。为防止已复现的旧版发布阻塞，仅在选稿入口排除不属于 Trending Daily 来源的直达 GitHub 仓库新闻，并为 dry-run 失败时保留稿件以便审稿；不改旧版写作提示词。
2. 检查回退差异与 Worker 配置等价性，运行旧版 Node 测试。使用固定 9 月 18 日缓存做不发布生成，核对每条新闻的事实、来源、图片、字数和发布校验。
3. 仅在预览可发布且人工审稿合格后，正常提交推送到 `main` 并用原工作流部署 Worker。只有新稿优于现有文章且图文无误时，才按原发布流程更新 9 月 18 日文章；否则保留现有好稿并记录阻塞。

回滚本次后端提交时使用普通 `git revert`，不得重置、强推或覆盖旧目录。

## 本地检查

- 旧版 Worker 的 77 项配置等价检查通过：45 项变量绑定与 32 项代码默认值合成后与历史配置一致，Cron 与 KV 等其它 TOML 部分不变。
- 旧版加来源入口小修后的 Node 测试 371/371 通过。原仓库当前的出版校验脚本引用了旧版不存在的商家模块，已一并恢复到旧版脚本，避免新旧版本混用。

## 第一轮自动稿审查

- [隔离试点 35321224834](https://github.com/dongyu19920904/CloudFlare-AI-Insight-Daily/actions/runs/35321224834) 使用 9 月 18 日固定缓存，`dailyWouldPublish=true`、`dailyPublished=false`。TOP 10 正文中位数 151 字，没有不足 80 字的条目，说明旧版写作密度已恢复。
- 人工审查发现三个可复现问题，因此没有部署：非 AI 的《Musk》纪录片进入 TOP；Jev 来源只写“成本大幅降低”，成稿却补成“十分之一/一个数量级”；强制高亮把裸 URL 写成 `https://**claude**.ai/artifacts`。
- 最小修正仅覆盖这三个边界：标题没有强 AI 信号的影视报道不入候选；定性变化不得扩写成定量数字并保留自述归属；裸 URL 属于高亮保护范围，同时清除 URL 内误插入的 Markdown 强调符。没有改变旧版 120–170 字、4–5 句的主体写法，也没有增加模型调用阶段或重试次数。修正后测试 373/373 通过。
- [边界修正试点 35413575921](https://github.com/dongyu19920904/CloudFlare-AI-Insight-Daily/actions/runs/35413575921) 再次使用 9 月 18 日缓存，结构校验通过且未发布。TOP 10 中位数 107.5 字、无不足 80 字条目；影视条目、无来源成本数字和破损 URL 均消失。审稿仍发现 Jev 同一发布事件被两个不同来源重复占位，因此在已有事件键中补入 Jev 与 Claude Code Projects，并把同事件去重前移到候选选择。完整测试 374/374 通过。
- 下一次隔离试点改用 9 月 19 日当天缓存，直接验收将要重发的当日素材。若自动稿通过结构与人工内容审查，再部署后端并由部署后的 Worker 自动重发 9 月 19 日；不手改 Markdown。
