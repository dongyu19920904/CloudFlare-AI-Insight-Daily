# 趣闻必写与短句优化：验证记录

## 范围

- 基线：`af95a08fd`。实现：`c4fce3c8e`，实稿问题后的提示词补丁：`1990a4a53`、`bdc10fd16`。
- 主要修改初写、repair、摘要和独立趣闻的提示词；撤销按“动作+反差”关键词拒收趣闻的规则，保留广告招领、原始链接和产品错配保护。
- 正常成稿必须有一条完整趣闻；不使用通用兜底，也不把 API 异常变成整个日报的发布阻塞。沿用既有单次补写，不增加处理阶段。
- 不改模型、Cron、Worker 变量、前端、AI 商机或账号商机；没有增加逐条联网核验或模型事实卡片流程。

## 本地验证

- 完整 Node 测试：507/507 通过，提示词补丁后重新全量通过。
- 配置等价检查：77 项配置（45 个文本绑定、32 个代码默认值）等价，其他 TOML 区段未变。
- `git diff --check` 通过。所有测试和下载使用 D 盘缓存包装器，本次临时目录由包装器清理。
- 未修改前端，未运行 Hugo 或浏览器视觉验收；不将后端测试当作前端验收。

## 第一次真实预览

- 工作流：`34623126513`，运行代码 `c4fce3c8e`，耗时 4 分 25 秒。
- 使用 2026-09-11 缓存日期；2026-09-12 缓存为空。预览没有回写素材或发布文章。
- `dailyGenerated=true`，`dailyWouldPublish=true`，`dailyPublished=false`，结构校验通过。
- TOP 10，独立补写后有趣闻。人工发现趣闻补入了原帖没有的其他系统/快捷键背景，不作为合格内容展示或发布。
- 追加提示词补丁，将共同趣闻规则放入独立补写的系统提示词，明确不得用额外技术背景制造笑点；仍不添加新流程。

使用仓库现有分析器对比上一轮相同缓存日期的预览（不是严格控制模型随机性的实验）：

| 指标 | 上一轮预览 | 第一次新预览 |
| --- | --- | --- |
| TOP 数量 | 10 | 10 |
| TOP 句数 | 42 | 83 |
| TOP 超过 55 字句数 | 5 | 1 |
| TOP 句长 P90 | 63 | 36 |
| TOP 最长句 | 83 | 63 |
| 全文超过 55 字句数 | 5 | 4 |
| 全文最长句 | 83 | 63 |

统计排除链接 URL、图片、标题和代码块，保留链接文字。现有分析器按中文/字母/数字计数，不能等同浏览器所有可见字符，也不统计代码块内的三行摘要。短句改善不代表可读性或事实真实性完全合格。

## 第二次预览

- `1990a4a53` 部署后的工作流 `34623997678` 在 3 分 46 秒结束并失败。响应只有 running 心跳，没有 success/error 最终记录，不能视为内容校验失败，也不能确定根因为超时；没有产出可审阅预览。
- 本地没有可用的状态查询凭据，未为此增加工作流或改生产执行机制。几分钟后只重试一次 dry-run：`34624778910`，保留第一次质量不合格样本。
- 本轮没有增加调用环节；但正常必写要求恢复了原有漏写补写的使用机会。首轮实际调用了一次独立趣闻补写，因此不能声称消耗不增加。
- 重试 `34624778910` 成功，3 分 45 秒。TOP 10、有趣闻、未发布；全文句长 P90 36、最长 65、超过 55 字 1 句，TOP 同为 1 句超过 55 字。仅返回社媒 1/2 条的质量警告。
- 人工仍不接受该趣闻：又写了桌面快捷键抢占，并且与 TOP 的同一事件重复。检查发现独立补写此前没有收到已用标题，只按 URL 排除候选，另一个 URL 的同一事件仍可被选中。
- `bdc10fd16` 仅把已有三级标题追加到独立补写输入，要求不重复已报道事件；不传全文、不额外调用模型、不新增关键词拒收。移除具体快捷键负面示例，避免反复诱导同一情节。

## 最终补丁的验收状态

- `bdc10fd16` 完整测试仍为 507/507 通过，部署工作流 `34625471589` 成功。
- 最后一次 dry-run `34625548887` 在 4 分 2 秒结束，仍只有 running 心跳而没有最终记录，没有生成可下载的预览。不能声称最新补丁的真实出稿已验收通过。
- 尝试本地 Wrangler 只读身份查询以继续读取远端状态，OAuth 刷新请求遇到 `UND_ERR_CONNECT_TIMEOUT`，未能获取状态。未修改网络配置或凭据，没有继续无限重跑。
- 本轮共四次 dry-run：两次返回完整稿，两次只有心跳后响应结束。完整稿均有 TOP 10 和趣闻，但趣闻人工质量仍未通过；最新的已用标题上下文修正等待真实结果验证。
- 最后成功样本保留于 `D:\GitHub\_artifacts\daily-fun-short-sentences-20260912-checked\daily-preview.md`，对应 `1990a4a53`，不是最新 `bdc10fd16` 的成稿。原始响应保留同目录 `trigger-final.json`。
- 下一次验证应先读取任务状态或执行一次受控 dry-run，重点核对趣闻是否换用未报道事件、是否不再新增无来源技术背景；不得把“结构校验通过”当成这些要求已满足。

## 边界与回滚

- 不承诺提示词能让模型永不违反规则，也不承诺外部 API 永不失败。
- 趣闻要求是必写，事实不能为笑点让步。异常仍采用原有隔离，避免主日报停发。
- 首轮还存在开源项目数量、高亮不足、来源限定和局部生硬断句等问题，未用新的全篇硬校验掩盖。
- 当前部署版本：`34bf26fe-3b45-46c6-aae5-efbbba419bb7`，部署工作流 `34625471589` 成功；Cron 不变。
- 必要时只撤销本轮三个实现提交，不 reset 原分叉目录，不退回旧的超限变量配置。文档提交不涉及运行行为。
- 预览前和最终检查线上 `daily/2026-09-11.md` blob 均为 `42bd09487983cda38b1cfb0e7f1045c5408f3025`，未被本次预览覆盖。
