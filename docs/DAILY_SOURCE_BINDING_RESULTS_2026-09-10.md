# 来源绑定修复：实施和真实试稿结果

## 完成情况

已按同目录方案完成最小后端修改、测试、SSH 推送、Worker 部署及一次真实不发布试生成。原始分叉工作区、前端 UI、定时配置和其他栏目没有由本任务修改。今天线上文章未被覆盖。

本次代码提交：`ec7a0dd77`。
保留其他任务账号商机更新后的部署提交：`462adbb8ddcc36abea568a75479ebee00f2c7d34`。
Worker 版本：`a226132c-e7fc-4817-96d0-78830de642af`。

## Diff 摘要

- `dailyPromptSelection.js`：社媒候选加入真实标题；返回入选正文和趣闻的结构化来源记录，包括无图素材。
- `dailyGenerationPromptInput.js`：一条候选内事实、来源和媒体必须绑定的说明，其余写作规则不变。
- 新增 `dailySourceBinding.js`：保守检查跨事件链接冲突；明确冲突时隔离单条而非猜测替换 URL；发生 TOP 隔离时就地整理编号；FAQ、代码示例和不确定情况保留。
- `handlers/scheduled.js`：仅在独立 daily 初稿、既有 repair、独立趣闻三个位置接入，初稿及 repair 均在补图和摘要之前检查。输出 `dailySourceBindingChecks`。
- 新增 13 项来源绑定测试及本方案、结果文档。

本任务未修改 cron、抓取逻辑、模型、发布安全下限、前端或两个商机的业务路径。部署合并里有其他任务新增的 merchant 文件和配置，均原样保留，不属于本任务修复。

## 验证结果

- 本次基线全量测试：445/445 通过。
- 合并远端账号商机更新后：461/461 通过，包含新增13项来源测试以及定时路由、任务隔离、媒体、自然链接和阅读质量测试。
- `git diff --check` 通过。首次测试发现旧去重函数并不处理普通缺号，已在新隔离函数中修正并加断言。
- 前端无修改，本次未运行 Hugo 构建或浏览器布局回归；Worker 打包部署通过。
- [部署任务](https://github.com/dongyu19920904/CloudFlare-AI-Insight-Daily/actions/runs/34459992040) 成功。
- [真实 dry-run](https://github.com/dongyu19920904/CloudFlare-AI-Insight-Daily/actions/runs/34460287576) 成功，用时 3 分 34 秒。
- `dailyGenerated=true`、`dailyValidationPassed=true`、`dailyWouldPublish=true`、`dailyPublished=false`。
- 当天前端文件 SHA 在试运行前后均为 `722d0078aab811179340bba9d9334398a836b179`。
- 当天文章的 GitHub 发布源保持原内容；[公开页面](https://news.aivora.cn/2026-09/2026-09-10/) 的本地 HTTP 复查遇到 TLS EOF/超时，另一公开读取工具也未能读取，因此本次不报告页面 200 或浏览器验收通过。这不能单独证明网站故障。

## 真实试稿观察

完整试稿：`D:\GitHub\_artifacts\daily-source-binding-20260910\daily-preview.md`。
机器报告：`D:\GitHub\_artifacts\daily-source-binding-20260910\trigger-final.json`。

本次复用缓存素材；主候选19条（项目4、新闻9、社媒6），趣闻候选12条。结果为 TOP10、开源2条、社媒3条，含趣闻，媒体统计12个（包括视频，不等同于12张图片）。

DeepSeek 出现在社媒区，链接为 `https://x.com/Gorden_Sun/status/2097607912076272029`；Omnii 位于 TOP5，链接为 `https://x.com/Gorden_Sun/status/2097635961614270891`，配图仍是对应的 `HRxMxNtbkAAo0r1`。未复现上一版 DeepSeek 挂 Omnii 来源和图片的问题。

本次初稿及 repair 的隔离记录均为空：这证明实际流程接入且本次未触发规则，不代表真实生成已经测到了隔离分支。隔离分支由固定回归测试验证，覆盖上一版错配形态；不能把一次随机正常输出说成以后绝不会串线。

## 未通过的编辑质量项

程序允许发布不等于编辑质量全部达标。本次仍保留为试稿，不覆盖线上好内容：

1. 长句提示：98句中6句超过55个可见字符，P90为51，最长66。
2. 5条 TOP 的短关键词突出不足，`dailyRepairMetQualityTargets=false`。
3. 趣闻变成 Codex 额度邀请信息，缺乏情节、反差和趣味；仅有栏目不代表有趣。
4. 有重大结论仍主要链接社媒转述，标题和摘要表达过于确定，例如“OpenAI 破解千禧年大奖难题”；本轮没有获得足够一手证据，不应标记为已核实事实。
5. AlphaGenome FAQ 将预测数据库写成替代实验验证，属于应优先核实和收窄的结论；来源绑定检查不能代替科学结论的证据审查。

这些不足列为后续编辑质量任务，不在本轮暗中扩大 prompt、事实校验或发布时间范围。

## 成本、隔离与回滚

新检查仅在内存中执行，不增加抓取请求、模型调用次数或重试次数；增加的标题和绑定说明会略增输入 token，未测得精确计费差额。本次试运行仍用了原有一次 repair。规则隔离后如果有效条数低于既有最低值，仍遵守既有安全发布规则，不保证任何坏稿都强行发布。

测试和下载运行于 D 盘缓存封装，临时目录由封装清理；D 盘试稿证据、工作区和共享依赖缓存保留。

需要回滚时，只在最新远端新工作区 `git revert ec7a0dd77`，回归测试后按原流程部署。不要整仓回退到旧部署提交，避免撤销另一任务的商机更新。

本规则不覆盖全部中文别名、同一产品不同事件、未知外链和复杂语义；仅是保守串线防护，不是通用事实核查器。
