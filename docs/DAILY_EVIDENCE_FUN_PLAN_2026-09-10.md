# 下一阶段：证据限定与趣闻选材最小修复

## 基线与保护

基线为后端 `e4f876fa7`，独立分支 `codex/daily-evidence-fun-20260910`，工作区 `D:\GitHub\_worktrees\daily-evidence-fun-20260910`。原始分叉目录及上一阶段记录均保留。不改前端 UI、定时配置、两个商机、模型、抓取数量和现有发布下限。

本轮不覆盖 9 月 10 日已发布文章。测试后按现有流程部署，再做一次真实 dry-run，保留完整试稿和剩余问题。

## 观察到的具体问题

1. 上次未发布试稿将社媒转述的重大研究写成直接确认，标题、开头、摘要的确定程度逐级升高。正文已有一般归属规则，但缺乏研究领域的具体边界；摘要提示词没有明确禁止丢掉限定。
2. 预测数据库被写成替代实验验证。FAQ 同样没有明确区分预测、实验、临床应用。
3. 趣闻输出 Codex 额度邀请帖。`scoreDailyFunCandidate` 对社媒、图片、Codex 等词加分，并不要求实际反常行为；`normalizeStandaloneDailyFunSection` 仅检查标题和来源链接，福利帖也能通过。
4. 短关键词不足另有现成规则和后处理，本轮不同时改字体、高亮算法或全文长度，避免多个变量掩盖原因。

## 本次一手核查

- [OpenAI 研究原文](https://openai.com/index/navier-stokes-solution/)：确有 2026-09-08 的研究发布及论文/形式化证明链接。因此问题不是“没有官方发布”，而是日报模型当时的输入只有社媒转述，不能宣称自己完成核实，更不能自行补写获奖、独立认可等状态。
- [DeepMind Atlas 原文](https://deepmind.google/blog/alphagenome-atlas-a-predictive-map-of-every-possible-dna-letter-change-in-the-human-genome/)：定位为预测资源，帮助确定后续实验验证的方向；不能压缩成“不必实验”。

这些核查用于制定编辑边界，不把当天文章和 URL 硬编码到每天生成代码中，也不让模型补写输入未提供的一手来源。

## 修改方案与文件

### 1. 正文和摘要保留同一证据等级

- `src/prompt/summarizationPromptStepZero.js`：针对研究增加“提交证明不等于独立认可；预测不等于实验或临床结论；统计相关不等于因果”。标题、首句、正文和 FAQ 都须遵守，但不要每句重复媒体姓名或机械免责声明。
- `src/prompt/summarizationPromptStepThree.js`：三句摘要必须保留“团队称、案例自述、预测”等必要限定；无法短而准确地概括时换另一条已支持事实，不删掉限定凑字数，不在综合主线时制造超出来源的结论。
- `src/handlers/scheduled.js`：只给已有 repair 提示词补相同证据边界；不新增整篇硬校验或模型调用。

### 2. 邀请帖不能靠截图进入趣闻

- `src/dailyFunSection.js`：小型纯函数识别“领取/邀请 + 额度/名额 + 留邮箱/私信”等明确招领意图，多个条件组合判断；只对趣闻使用，不把 Codex、额度或免费等单个词拉黑。
- `src/dailyPromptSelection.js`：趣闻池排除这类明确招领帖；同分时为真实动作与反常结果的组合加分，不改主新闻筛选和栏目数量规则。
- 正文与独立趣闻提示词同步要求真实情节，而不是把领取步骤、工具宣传、版本发布硬凑成包袱。保留“无合格素材可以不写”，不生成兜底。
- 初稿及现有 repair 在摘要之前只清除明显招领型趣闻；独立趣闻也做相同处理。正常新闻、FAQ、来源和图片不变，剩余日报仍走原发布流程。

### 3. 最小测试

新增专门测试文件，覆盖：正文/摘要/repair 证据边界提示保持一致；邀请码与额度招领被挡在趣闻之外；含额度词的真实操作经历、普通邀请协作不误伤；清除坏趣闻后其他栏目逐字不变；独立趣闻全不合格返回空；来源绑定与定时/商机隔离回归。

提示词断言仅证明规则被传入，不证明模型事实正确。最终必须人工看真实试稿，保留未达标项。

## 验证和发布

按 D 盘缓存规则跑针对性测试、全量 Node 测试和 diff 检查。不改前端，本轮无需 Hugo/CSS 构建。SSH 非强制推送，现有 Actions 部署；若远端变化则先检查并保留其他任务改动。

运行一次 `verify-image-recovery.yml`：日期 `2026-09-10`、`dry_run=true`。检查 TOP 数量、证据限定、趣闻选材、媒体和日志，并核对线上日期文件 SHA 未变。

不新增抓取或事实核查网络调用，不增加模型调用上限；新增提示文字会略增输入 token。坏趣闻被移除后，可能使用原本已有的独立趣闻调用，而非新增一次调用。

回滚仅 revert 本次代码提交，不整仓回退，不撤销同期商机更新。

## 验收边界

做到：不再将明确招领帖直接当趣闻；提示词端补上从正文到摘要的证据限定；正常日报链路、来源绑定、页面样式和定时保持原行为。

不声称做到：自动核实所有事实、保证每天一定有趣闻、保证一次试稿全部优秀。若真实试稿仍越过证据边界，本轮不发布试稿，列出实际问题，不靠提高整篇失败率解决。
