# 证据限定与趣闻优化：已完成代码，部署受阻

## 已完成

按同目录 `DAILY_EVIDENCE_FUN_PLAN_2026-09-10.md` 修改了5个源文件，新增11项测试；方案、代码、测试已通过 SSH 推送至 main。

代码提交：`c3873a2b62b928c2f6c4bfccd26742390387ea77`。

- 正文、repair、三句摘要明确保留研究限定；不把预测写成实验结论，不把公布证明稿写成独立认可。
- FAQ 不得把预测数据库写成替代实验或诊疗。
- 趣闻候选排除明确的“领取/邀请 + 额度/名额 + 留邮箱/私信”等组合招领意图，不因 Codex 或额度单个词过滤。
- 趣闻初稿、repair 和独立生成均做局部防护；只移除坏趣闻，正常正文和链接不变。
- 真实动作与反常结果组合在趣闻评分中小幅加分。没有合格趣闻可以省略，不生成兜底。

## 验证

- Node 全量测试：472/472 通过（原有461项 + 本次11项）。
- 覆盖独立趣闻空返回、招领组合识别、普通额度经历不误伤、其他栏目逐字不变、来源绑定、定时和商机回归。
- 首次针对性测试发现趣闻加分补丁误落到同名条件的普通评分位置，已在发布前纠正；最终 diff 仅改趣闻评分，全量测试通过。
- `git diff --check` 通过。
- D 盘进程缓存封装运行测试并清理本次临时目录；工作区和共享依赖缓存保留。
- 前端未改，无 Hugo 或浏览器布局验收。
- 本轮没有修改 `wrangler.toml`、部署 workflow、cron、主站或商机业务逻辑。
- 今天前端发布源文件 SHA 前后均为 `722d0078aab811179340bba9d9334398a836b179`，未覆盖文章。

## 部署失败：不是代码测试失败

[部署任务 34498176575](https://github.com/dongyu19920904/CloudFlare-AI-Insight-Daily/actions/runs/34498176575) 在 Cloudflare 上传版本接口被拒绝，错误码 `10055`：现有 Worker 共85个变量，超过当前 Workers Free 的64个变量上限。

[Cloudflare 官方限制说明](https://developers.cloudflare.com/workers/platform/limits/) 确认变量数量计入 secrets 与普通文本变量，Free为64、Paid为128。

本地 `[vars]` 有77个普通变量；85是部署接口报告的总数，不通过打印或读取密钥值核对。与上次成功部署提交 `462adbb8d` 对比，`wrangler.toml` 和部署流程无差异，本次没有增加变量。为什么相同配置此前可部署、当前按免费上限拒绝尚未确认，不擅自认定用户套餐变化。

没有升级套餐、删 secrets 或改线上配置。不能把“代码已推送”称为“新版本已上线”。未触发真实生成，以免测试旧版、浪费调用并误报本轮效果。最近成功部署记录仍为上一阶段；本轮未做线上运行版本查询。

## 下一步配置修复建议（待确认，不在本轮实施）

优先尝试不增加付费、保持实际参数值不变的配置精简：

1. 建立配置使用清单，逐项区分启动必填、模板读取、计划任务读取、真正默认值与备用模型开关，不能仅搜索一次 `env.NAME` 就判定可删。
2. 优先省略与代码默认值完全一致的非必填变量，例如重试默认项、空备用基址、关闭的页脚开关等；逐项验证缺省前后行为一致。
3. 不动凭据、定时表达式、来源ID、GitHub目标和当前模型线路，不把关闭的备用开关随便删除（有的缺省值反而是开启）。
4. 注意 `FOLO_COOKIE_KV_KEY`：虽然读取函数有默认值，但 `src/index.js` 仍把它作为启动必填，HTML管理页也直接引用，所以不能直接删。播客字段也在必填数组中，不能因本轮没用播客就删。
5. 至少净减少21个变量才到64，应留出余量。若纯默认项不够，需要评估集中非敏感配置或兼容默认解析的改动；这会触及所有任务共用的配置边界，必须单独测试并说明范围。
6. 验证主日报、账号商机、普通商机、手动入口和备用线路在等价配置下行为一致，再部署。不要为凑数删配置而损坏日报自动运行。
7. 成功部署后再执行本方案原定的一次真实 dry-run，展示完整试稿及编辑质量问题，不直接覆盖线上文章。

另一个选择是用户自行确认/恢复合适的 Workers 付费套餐，但不默认建议付费，也不代替用户修改账单。

## 回滚与未验证项

本次代码可通过 revert `c3873a2b6` 撤回，不整体回退仓库，也不撤销其他任务提交。配置上限问题并不是本次代码造成，revert这次提交也不会减少85个现有变量。

未验证：新版本真实生成、摘要实际限定保留率、趣闻实际趣味、线上运行版本。提示词测试只说明规则存在，不代表模型一定遵循；招领识别是保守启发式，不是通用语义分类器。
